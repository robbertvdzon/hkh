apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: frontend
spec:
  host: hkh.vdzonsoftware.nl
  to:
    kind: Service
    name: frontend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
